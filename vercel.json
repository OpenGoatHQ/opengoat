{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd site && npm install && npm run build",
  "outputDirectory": "site/.next",
  "installCommand": "echo 'install handled by buildCommand'",
  "framework": "nextjs",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./site ./humans ./data ./scripts"
}
